# environments
.venv/
__pycache__/
*.pyc

# secrets
.env

# the decoded-log Parquet fixture in data/ is committed on purpose so the notebook runs offline

data/*.json
!data/transfers_*.meta.json


# jupyter
.ipynb_checkpoints/

# committed on purpose: assets/*.png (so charts render in the README without running),
# and the executed notebooks/*.ipynb (so GitHub shows the output inline).
